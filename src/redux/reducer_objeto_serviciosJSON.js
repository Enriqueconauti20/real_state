import {objeto_serviciosJSON} from '../shared/serviciosJSON';

export const fun_Pura_Reducer_obje_serviciosJSON = (state=objeto_serviciosJSON,  action) =>{
    switch(action.type){
        default:
            return state; 
    }
}